type: shell
name: atuin
commands:
  - LATEST_URL=$(curl -s https://api.github.com/repos/atuinsh/atuin/releases/latest | grep "browser_download_url.*x86_64-unknown-linux-gnu.tar.gz" | grep -v ".sha256" | cut -d '"' -f 4)
  - curl -L -o /tmp/atuin.tar.gz "$LATEST_URL" && tar -xzf /tmp/atuin.tar.gz -C /tmp
  - EXTRACTED_DIR=$(tar -tzf /tmp/atuin.tar.gz | head -1 | cut -f1 -d"/")
  - sudo mv /tmp/$EXTRACTED_DIR/atuin /usr/local/bin/
  - rm -rf /tmp/atuin* /tmp/$EXTRACTED_DIR
