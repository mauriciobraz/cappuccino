type: shell
name: uv
commands:
  - LATEST_URL=$(curl -s https://api.github.com/repos/astral-sh/uv/releases/latest | grep "browser_download_url.*x86_64-unknown-linux-gnu.tar.gz" | grep -v ".sha256" | cut -d '"' -f 4)
  - curl -L -o /tmp/uv.tar.gz "$LATEST_URL" && tar -xzf /tmp/uv.tar.gz -C /tmp
  - EXTRACTED_DIR=$(tar -tzf /tmp/uv.tar.gz | head -1 | cut -f1 -d"/")
  - sudo mv /tmp/$EXTRACTED_DIR/uv /usr/local/bin/
  - rm -rf /tmp/uv* /tmp/$EXTRACTED_DIR
